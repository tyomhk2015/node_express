extends layouts/main-layout.pug

block content
  main
    h1 ADD IDOLS (PUG+LAYOUT)
    form(action="/add/idol", method="POST")
      input(type="text", name="idol")
      button(type="submit") Register Idol!